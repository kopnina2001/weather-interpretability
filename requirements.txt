# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
# torch/torchvision/torchaudio need the cu126 index for GPU wheels on this cluster's driver (580.173.02, V100 SXM3).
# onnxruntime-gpu needs LD_LIBRARY_PATH pointed at torch's bundled CUDA libs (see venv/bin/activate PANGU_LD_FIX
# block, or the first cell of 02_saliency_experiment.ipynb) — a system CUDA toolkit is not required separately.

torch==2.13.0+cu126
torchvision==0.28.0+cu126
torchaudio==2.11.0+cu126

xarray==2025.6.1
zarr==2.18.3
gcsfs==2026.7.0
fsspec==2026.6.0
dask==2026.7.1
numpy==2.2.6
pandas==2.3.3

matplotlib==3.10.9
Cartopy==0.25.0

onnxruntime-gpu==1.23.2
captum==0.9.0
gdown==6.1.0

jupyter==1.1.1
ipykernel==7.3.0
